# PROMPTS — open slots

No open slots: every beat is a deterministic Remotion render. No generated
images, no paid calls, no pantry requests. Library-first searches run before
authoring (`./art scenes …` for bar charts, equations, counters, strike-through,
tables, code) found no temperature/softmax component, so seven reel-local
components were built in `runtime/remotion/src/TemperatureConcentration.tsx`
(copy in `remotion-src/`).
